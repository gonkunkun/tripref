export default () => {
  // export default ({ req, store }) => {
  //  TODO: 認証処理実装後に、ログイン済みかどうか確認する処理を追加
  // if (process.browser) {
  //   return
  // }
  // const cookies = new Cookies(req.headers.cookie)
  // const user = cookies.get("user")
  // if (user && user.id) {
  //   const { id, likes } = user
  //   store.commit("setUser", { user: { id, likes } })
  // }
}
